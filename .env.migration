# Configuration des bases de données pour la migration
# La base de données source (actuelle)
OLD_DATABASE_URL="postgresql://gestionmax:supersecurepassword@localhost:5432/gestionmax_local"

# La base de données cible (nouvelle structure)
# Par défaut, on utilise la même base mais vous pouvez utiliser une base différente
NEW_DATABASE_URL="postgresql://gestionmax:supersecurepassword@localhost:5432/gestionmax_local"

# Variables d'application standard
DATABASE_URL="postgresql://gestionmax:supersecurepassword@localhost:5432/gestionmax_local"
NEXTAUTH_SECRET="IaQdE9fU8hP7mC2vX6yT3zJ5nB4rW1oL"
NEXTAUTH_URL="http://localhost:3000"
